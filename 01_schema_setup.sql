CREATE TABLE Executive_Master (
    Executive_ID INT PRIMARY KEY,
    Executive_Name VARCHAR(50),
    Training_Status VARCHAR(20),
    Department VARCHAR(30)
);
-- Setup: Initialize database schema
Add 01_schema_setup.sql to define the base table structures for Executive Master and Operational Logs.
